<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <AssemblyName>Viset.Serialization</AssemblyName>
    <RootNamespace>Viset.Serialization</RootNamespace>
    <ImplicitUsings>enable</ImplicitUsings>
    <!-- Tomlyn emits an unreachable fallback after handling extension data in init-only models. -->
    <NoWarn>$(NoWarn);CS0162</NoWarn>
    <RestorePackagesWithLockFile>false</RestorePackagesWithLockFile>
    <RestoreLockedMode>false</RestoreLockedMode>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Tomlyn" />
  </ItemGroup>
</Project>
